# ===== AI Travel Agent - core dependencies =====
langchain>=0.3.0
langchain-core>=0.3.0
langchain-deepseek>=0.1.4        # DeepSeek API official LangChain integration
langgraph>=0.2.0                 # multi-node agent workflow orchestration
langchain-text-splitters>=0.3.0  # localized Chinese text splitter

pydantic>=2.7
python-dotenv>=1.0
requests>=2.31
numpy>=1.24

# ===== Demo UI (optional) =====
streamlit>=1.36

# greenlet<3.2 ships prebuilt wheels for Python 3.9; >=3.2 needs MSVC build
greenlet>=1,<3.2; python_version < "3.10"

# ===== Local Chinese embedding (optional, recommended) =====
# After install, BAAI/bge-small-zh-v1.5 will be used automatically;
# otherwise the built-in lightweight local hash embedder is used.
# sentence-transformers>=2.7

# ===== Vector database (optional, built-in lightweight store by default) =====
# chromadb>=0.5
